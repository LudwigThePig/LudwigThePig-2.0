/**
 * This file doesn't do anything, any more, at this moment... 
 * but it will be filled out later!
 */

import { navPayload } from "./navTypes";

export interface State {
  activeNav: navPayload;
}
